// 聲明時給出初始值，會自行推斷屬性的類型。
class Point {
    x = 0;
    y = 0;
  }

//difference is that a class contains "method implementation", whereas type aliases and interfaces don't
//類型別名和介面無法"實例化"，它們僅包含"結構資訊"。

// readonly
// private
//  protected